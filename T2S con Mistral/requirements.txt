# Allineato a pyproject.toml (dipendenze runtime).
# Chatterbox installa anche torch e molte librerie transitivamente.
#
# Solo librerie (senza CLI locale):
#   uv pip install -r requirements.txt
#
# Progetto + CLI voxtral-backend + test (consigliato):
#   uv pip install -e ".[dev]"

datapizza-ai==0.1.0
huggingface_hub>=1.8.0
httpx>=0.28.1
mistral-common>=1.10.0
numpy>=1.26,<3
python-dotenv>=1.2.2
soundfile==0.13.1
